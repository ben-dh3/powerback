export const {
    ProfilePane,
    ProfileSubPane,
    ProfileSideNav,
  } = require('./Profile'),
  SettingsPane = require('./Settings').default,
  CelebrationsPane = require('./Celebrations').default;
