export type PolData = {
  middle_name: string;
  first_name: string;
  start_date: number; // of days since
  last_name: string;
  district: string;
  end_date: number; // of days until
  chamber: string;
  twitter?: string;
  FEC_id: string;
  state: string;
  name: string;
  id: string;
};
